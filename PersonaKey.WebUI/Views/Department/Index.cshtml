@model IEnumerable<PersonaKey.EntityLayer.Concrete.Department>

@{
    ViewData["Title"] = "Departmanlar";
}

<div class="bg-white rounded-lg shadow p-4 max-w-full overflow-auto">

    <h2>@ViewData["Title"]</h2>

    <a asp-action="Create" class="btn btn-primary mb-3">Yeni Departman Ekle</a>

    <table class="table table-striped mb-0">
        <thead>
            <tr>
                <th>Adı</th>
                <th>İşlemler</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Name</td>
                    <td>
                        <a asp-action="Update" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Düzenle</a>
                        <a href="#" class="btn btn-sm btn-danger">Sil</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>